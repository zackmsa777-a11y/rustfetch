# Winget

Official winget submission needs a Microsoft Store / winget-pkgs PR after a signed Windows release.

Until then, Windows users can:

```powershell
cargo install rustftechh --locked
```

Or scoop bucket: https://github.com/zackmsa777-a11y/scoop-rustftechh
